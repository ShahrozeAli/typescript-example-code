let a: number;
let b: boolean;
let c: string;
let d: any;
let e: number[] = [1, 2, 3]; //number array
let f: any[] = [1, true, "a", false]; //any array

enum Color { //enum in ts
  red,
  green,
  blue,
}

let bgColor = Color.green;
